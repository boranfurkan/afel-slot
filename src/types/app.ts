export enum GameTabs {
  SLOT = 'SLOT',
  FLIP = 'FLIP',
}

export enum SlotIconType {
  MEAT = 1,
  CROCODILE = 2,
  HEAD = 3,
  TRUMP = 4,
  AFEL = 5,
  SOLANA = 6,
}

export type FlipSide = 'HEADS' | 'TAILS';
